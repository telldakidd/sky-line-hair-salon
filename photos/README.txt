Generated. Do NOT put photos here — this whole folder is deleted on every build.

Put them in site-factory/assets/sky-line-hair-salon/photos/ instead, named 1.jpg through
6.jpg (roughly square, ~1000px), plus team-1.jpg, team-2.jpg for staff.
They are copied in here at build time, so they survive a rebuild.

Until a photo exists each tile shows a labelled placeholder, so the site never
looks broken while you are waiting on the client.
